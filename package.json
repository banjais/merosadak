{
  "name": "merosadak-snapdeploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --omit=dev && npm run build",
    "start": "cd backend && npm run start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
