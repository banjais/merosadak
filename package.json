{
  "name": "merosadak-snapdeploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm run start",
    "build": "cd backend && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
